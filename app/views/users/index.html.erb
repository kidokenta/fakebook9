<p class = "userindextitle">USER INDEX</p>
<% @users.each do |user| %>






 <div class = "post">

<%# -----------------------ここから投稿の上部分-------------- %>
            <div class = "postcontent">
                  <div class = "postcontentuser">
                       <div class = "imageandname">
                           <div class = "postuserimage">
                            <%= image_tag user.image.url, :size => '70x70',class: "userimage" if user.image.present?%> </div>
                           <div class = "postusername"> <p><span></span><%=link_to user.username, user_path(user.id),class: "fortextdeconone" %></p> </div>
                      </div>
                      <div class = "dataandfunction">
                      <%# ------------- %>
                      <% if current_user != user %>
        <% if user.followed_by?(current_user) %>
            <p><%=link_to "フォロー済み", user_relationships_path(user.id), method: :delete %></p>
        <% else %>
            <p><%=link_to "フォローする", user_relationships_path(user.id), method: :post %></p>
        <% end %>
    <% end %>

                   <%# -----------------------%>
                      </div>
                 </div>
            </div>


     </div>

     <% end %>